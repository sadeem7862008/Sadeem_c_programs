#include <stdio.h>
int main() {
    int i,n,sum=0;
    printf("enter a positive integer:");
    scanf("%d",&n);
    for(i=1;i<=1;i++)
    sum=sum+i;
    printf("sum of n natural number:%d",sum);
    return 0;
}